{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "smartstock.serviceAccountName" . }}
  labels:
    {{- include "smartstock.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
